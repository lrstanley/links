# goreleaser.yml
build:
  binary: links
  goos:
    - darwin
    - windows
    - linux
    # - freebsd
  goarch:
    - amd64
    - arm
    - arm64
  goarm:
    - 6
    - 7
  hooks:
    pre: make prep
archive:
  format_overrides:
    - goos: windows
      format: zip
release:
  draft: true
snapshot:
  name_template: snapshot-{{.Commit}}
